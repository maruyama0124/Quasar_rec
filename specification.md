# 謎解きレクリエーションアプリ 仕様書

## 1. プロジェクト概要

サークル合宿（約50名参加）で使用する、スマートフォン向け謎解きゲームWebアプリケーション。

### ゲームの流れ

```
[Step 1] チーム選択
    ↓
[Step 2] 宿の中で主催者を探す（アプリ外のリアル行動）
    ↓
[Step 3] 主催者からクイズを出される → 正解したら一緒に記念撮影
         （写真内の手のポーズが次のヒントになる）
    ↓
[Step 4] ヒントをもとに宿のどこかにある封筒を探す（アプリ外）
    ↓
[Step 5] 記念撮影した写真をアプリにアップロード（思い出 & 証拠として保存）
    ↓
[Step 6] 封筒に書かれた暗号をアプリに入力
         → 正しければLINEトーク画面のスクショ1枚を獲得
    ↓
[Step 7] Step 2〜6を繰り返し、全7枚のスクショを集める
    ↓
[Step 8] 7枚のスクショをドラッグ&ドロップで正しい順番に並べ替え
         → 並べると1つのクイズが読める形になる
    ↓
[Step 9] そのクイズの答えを主催者に口頭で伝える → ゲームクリア
```

> **重要:** 並べ替えの正誤判定・最終回答の正誤判定はアプリ内では行わない。
> 主催者が目視で確認する運用。

---

## 2. 技術構成

| 項目 | 選定 | 理由 |
|------|------|------|
| 形式 | React 単一ファイル（`.jsx`） | Claude.ai Artifact として即実行可能 |
| スタイリング | Tailwind CSS | ユーティリティクラスのみで完結 |
| 状態管理 | React `useState` | 軽量で十分 |
| 進捗永続化 | `window.storage` API | リロードしても進捗が残る。サーバー不要 |
| 画像形式 | Base64 データURL埋め込み | 外部サーバー不要、オフラインでも動作 |
| 対象デバイス | スマートフォン（iOS / Android） | モバイルファースト設計 |
| 同時接続 | 約50人 | サーバーレスのため同時接続の制約なし |

### 進捗保持の設計

```
キー: "progress:{チーム英字名}"
値（JSON文字列）: {
  teamName: "チームかなで",
  teamKey: "kanade",
  unlockedCodes: ["CODE_A", "CODE_C"],
  photos: ["data:image/jpeg;base64,...", null, "data:image/jpeg;base64,...", ...],  // 7スロット、未撮影はnull
  imageOrder: [3, 1, 5, 0, 2, 6, 4],
  phase: "collecting"
}
```

- **collecting**: スクショ収集フェーズ（7枚未満）
- **arranging**: 全7枚揃った後の並べ替えフェーズ

---

## 3. 画面構成と詳細仕様

### 3-1. チーム選択画面（トップ）

**表示条件:** 初期表示 or ヘッダーの「戻る」から遷移時

#### UI要素

- アプリタイトル・簡単なルール説明テキスト
- 9チームの選択ボタン（2列グリッド配置）
- 既存の進捗があるチームには「続きから」バッジを表示

#### チーム一覧（9チーム）

| # | チーム名 | storageキー |
|---|---------|-------------|
| 1 | チームかなで | `progress:kanade` |
| 2 | チームまえじ | `progress:maeji` |
| 3 | チームくにちゃん | `progress:kunichan` |
| 4 | チームむた | `progress:muta` |
| 5 | チームやまちゃん | `progress:yamachan` |
| 6 | チームかいと | `progress:kaito` |
| 7 | チームけいた | `progress:keita` |
| 8 | チームひゅうが | `progress:hyuga` |
| 9 | チームわかな | `progress:wakana` |

#### 処理フロー

1. 画面表示時、各チームの `window.storage.get()` で保存済みデータを確認
2. 保存済みのチームにはバッジを表示
3. チーム選択ボタンをタップ → 確認ダイアログ表示
4. 確定後、進捗データがなければ初期化して `window.storage.set()` で保存
5. メイン画面へ遷移

---

### 3-2. メイン画面（暗号入力 / スクショ収集）

**表示条件:** チーム選択後 or 再開時（phase: `"collecting"`）

#### UI要素

- ヘッダー: チーム名 + 「チーム選択に戻る」ボタン
- **暗号入力エリア:** テキストinput + 「送信」ボタン
- **記念写真エリア:** 7枠のフォトスロット（撮影済みはサムネイル表示、未撮影は📷アイコン）
- **獲得スクショエリア:** サムネイル一覧（獲得順表示）+ 進捗表示「獲得: 3 / 7 枚」
- 未獲得スロットはプレースホルダー（?マーク）で表示
- 7枚揃ったら「並べ替え画面へ」ボタンを表示

#### 記念写真アップロードの処理

1. フォトスロット（📷アイコン）をタップ
2. スマホのカメラ or フォトライブラリから画像を選択（`<input type="file" accept="image/*" capture="environment">`）
3. 選択された画像をクライアント側でリサイズ（長辺600px以下）
4. Canvas API で JPEG 圧縮（品質0.7）→ Base64データURLに変換
5. 進捗データの `photos` 配列の該当スロットに保存
6. `window.storage` に保存
7. サムネイルとして表示

> **注意:**
> - 写真はあくまで記念・証拠用。ゲーム進行のロック条件にはしない
> - 暗号入力と写真アップロードは独立して操作可能（順番自由）
> - 写真タップで拡大表示（モーダル）
> - 写真の再撮影（差し替え）も可能

#### 暗号入力の処理フロー

1. テキスト入力欄に暗号を入力
2. 「送信」ボタンをタップ
3. 入力値を正規化:
   - 前後の空白除去（`trim()`）
   - 全角英数字 → 半角に変換
   - 半角英字 → 大文字に変換
   - ひらがな → カタカナに変換
4. 正規化した値を `CODE_MAP` のキーと照合
5. **一致 & 未獲得:** スクショ画像をモーダルで拡大表示 → 進捗に追加 → storage保存
6. **一致 & 獲得済み:** トースト「この暗号は入力済みです」
7. **不一致:** トースト「この暗号は正しくありません」
8. **7枚到達時:** phaseを `"arranging"` に更新 → storage保存

---

### 3-3. 並べ替え画面

**表示条件:** 全7枚獲得後（phase: `"arranging"`）

#### UI要素

- ヘッダー: チーム名 + 「暗号入力に戻る」ボタン
- 説明テキスト:「スクショを正しい順番に並べ替えてください！」
- 7枚のスクショカードを縦に並べたリスト
  - 各カードの右端にドラッグハンドル（≡アイコン）
  - カード本体タップ → 画像拡大表示
- 「この順番で確定！」ボタン → タップで「主催者に見せてください！」と表示

#### ドラッグ&ドロップの処理

1. **ドラッグハンドル長押しで開始** （カード本体タップは拡大表示用に確保）
2. `touchstart` / `touchmove` / `touchend` イベントで自前実装
   - **HTML5 DnD API は使わない**（モバイルSafariで非対応のため）
3. ドラッグ中: 対象カードを半透明＋影付きで浮かせる表現
4. `touchmove` でY座標を追跡し、他カードの位置と比較して挿入位置を決定
5. `touchend` で確定、配列を更新
6. ドラッグ中は `e.preventDefault()` でブラウザスクロールを抑制
7. 並び順が変わるたびに `window.storage` に自動保存

---

## 4. UIデザイン方針（謎解き感の演出）

### カラーパレット

| 用途 | 色 | イメージ |
|------|-----|---------|
| 背景 | ダークネイビー `#0a0e1a` 〜 `#1a1f35` | 夜・秘密の雰囲気 |
| アクセント | ゴールド `#d4a847` / アンバー `#f59e0b` | 古びた鍵・宝の地図 |
| テキスト | オフホワイト `#e8e0d0` | 古い紙・羊皮紙風 |
| 成功表示 | エメラルド `#10b981` | 解錠の光 |
| エラー表示 | クリムゾン `#ef4444` | 警告 |
| カード背景 | ダークブルー `#111827` + 微細なボーダー `#d4a847` | 封筒・手紙感 |

### ビジュアル演出

- **背景:** ダークグラデーション + 微かな星や光の粒子（CSS animation）
- **タイトル:** ゴールドの文字 + 微かなグロー（text-shadow）
- **暗号入力欄:** 古びた入力装置風（ボーダーをゴールド、背景を半透明ダーク）
- **ボタン:** ゴールド系グラデーション + ホバーで光る演出
- **スクショカード:** ダークカードに金縁ボーダー、獲得時にフェードインアニメーション
- **フォトスロット:** 破線ボーダー + カメラアイコン、撮影済みはポラロイド風
- **進捗表示:** 「🔓 3 / 7」のように鍵アイコンを使用
- **並べ替え画面:** カードにナンバリングなし（順番を推理させる）
- **確定ボタン:** 封蝋（シーリングワックス）をイメージした赤丸アイコン
- **トースト:** 半透明ダーク背景 + 金テキスト

### フォント

- メインテキスト: システムフォント（sans-serif）
- タイトル・見出し: Tailwind の `font-serif`（明朝系でミステリー感）

### アニメーション

- 画面遷移: フェードイン（200ms）
- スクショ獲得: スケールアップ + グロー効果
- トースト: 下からスライドイン → 2秒後フェードアウト
- ドラッグ中カード: scale(1.05) + shadow-xl + opacity-80

---

## 5. 共通UIコンポーネント

### 画像拡大モーダル

- スクショタップで画面全体にオーバーレイ表示
- 背景は半透明黒（`rgba(0,0,0,0.8)`）
- 画像は画面幅いっぱいに表示
- 背景タップ or 右上✕ボタンで閉じる

### トースト通知

- 画面下部に一時的なメッセージをスライドイン表示
- 2秒後に自動でフェードアウト
- 色分け: 成功=緑、エラー=赤、情報=青

### ヘッダー

- 常にチーム名を左に表示
- 右にナビゲーションボタン

---

## 6. データ構造

### 暗号マスターデータ（CODE_MAP）

```javascript
// ★★★ 後で差し替えるデータ ★★★
// キー: 暗号文字列（正規化後の形式: 大文字・カタカナ）
// 値: { id: スクショ番号(1-7), image: Base64データURL }

const CODE_MAP = {
  "AAAA": { id: 1, image: "data:image/png;base64,（ここにBase64文字列）" },
  "BBBB": { id: 2, image: "data:image/png;base64,（ここにBase64文字列）" },
  "CCCC": { id: 3, image: "data:image/png;base64,（ここにBase64文字列）" },
  "DDDD": { id: 4, image: "data:image/png;base64,（ここにBase64文字列）" },
  "EEEE": { id: 5, image: "data:image/png;base64,（ここにBase64文字列）" },
  "FFFF": { id: 6, image: "data:image/png;base64,（ここにBase64文字列）" },
  "GGGG": { id: 7, image: "data:image/png;base64,（ここにBase64文字列）" },
};
```

### 進捗データ（Storage）

```javascript
// window.storage に保存する形式
{
  teamName: "チームかなで",       // 表示用
  teamKey: "kanade",             // storageキー用
  unlockedCodes: ["AAAA", "CCCC"], // 入力済み暗号（正規化済み）
  photos: [                       // 記念写真（7スロット）
    "data:image/jpeg;base64,...", // スロット1: 撮影済み
    null,                         // スロット2: 未撮影
    "data:image/jpeg;base64,...", // スロット3: 撮影済み
    null, null, null, null        // スロット4-7: 未撮影
  ],
  imageOrder: [3, 1, 5, 0, 2, 6, 4], // 並べ替え画面でのスクショid順
  phase: "collecting"            // "collecting" | "arranging"
}
```

---

## 7. 画像差し替えガイド（主催者向け）

### 差し替え手順

#### Step 1: 暗号を7つ決める

封筒に書く暗号を7つ決める。日本語でもアルファベットでもOK。
例: 「さくら」「ふじさん」「かみなり」「にじ」「ほしぞら」「たいよう」「つき」

#### Step 2: スクショ画像をBase64に変換

**推奨:** 画像は横幅800px以下、JPEG品質80%程度にリサイズしてから変換。

オンラインツール: https://base64.guru/converter/encode/image
→ 「Data URI」形式（`data:image/jpeg;base64,...` の形）でコピー

#### Step 3: ソースコード内の CODE_MAP を書き換え

```javascript
const CODE_MAP = {
  "サクラ":   { id: 1, image: "data:image/jpeg;base64,/9j/4AAQ..." },
  "フジサン": { id: 2, image: "data:image/jpeg;base64,/9j/4AAQ..." },
  // ... 7つ分
};
```

> **注意:**
> - アプリ側で入力を「大文字・カタカナ」に正規化するため、
>   CODE_MAP のキーも「大文字・カタカナ」で記述すること
> - 例: ユーザーが「さくら」と入力 → アプリが「サクラ」に変換 → CODE_MAP の "サクラ" と照合

---

## 8. 非機能要件

| 項目 | 要件 |
|------|------|
| 対応ブラウザ | iOS Safari 16+ / Android Chrome 最新版 |
| 画面サイズ | 375px〜430px幅を主対象（モバイルファースト） |
| 同時利用人数 | 約50人（クライアント完結のため制約なし） |
| 進捗保持 | `window.storage` による永続化（リロード・ブラウザ再起動に耐性） |
| レスポンス | 暗号照合はクライアント内で即時完了 |
| タッチ操作 | ボタン最小サイズ44×44px、ドラッグ操作にハンドル設置 |
| 画像容量 | スクショ7枚 × 推奨200KB以下 = 合計1.4MB以下 |

---

## 9. 実装上の注意事項

1. **ドラッグ&ドロップはtouchイベントで自前実装する**
   - HTML5 DnD APIはモバイルSafariで動作しない
   - `touchstart` / `touchmove` / `touchend` を使用

2. **Base64画像の容量管理**
   - スクショは事前にリサイズ（横800px以下）＋JPEG圧縮を推奨
   - 合計1.4MB以下を目標

3. **暗号の正規化処理**
   - `trim()` → 全角英数→半角変換 → 小文字→大文字変換 → ひらがな→カタカナ変換
   - これにより「sakura」「SAKURA」「さくら」「サクラ」を同一視可能

4. **window.storage のエラーハンドリング**
   - 全操作を `try-catch` で囲む
   - 読み込み失敗時は新規データとして初期化

5. **チーム選択の誤操作防止**
   - 選択時に確認ダイアログを表示
   - 既存の進捗がある場合は「途中から再開します」と明示

6. **並べ替え画面での誤操作防止**
   - ドラッグはハンドル部分からのみ開始可能
   - カード本体タップは拡大表示に割り当て（ドラッグとの競合回避）

7. **記念写真の容量管理**
   - カメラから取得した画像はクライアント側で必ずリサイズ＋圧縮
   - Canvas API で長辺600px以下、JPEG品質0.7に変換
   - 1枚あたり約50〜100KB → 7枚で350〜700KB
   - `window.storage` の1キーあたり5MB制限に注意（スクショBase64を進捗データに含めない設計）

8. **写真とスクショの保存先分離**
   - 記念写真は容量が大きいため、進捗データとは別キーで保存を推奨
   - キー例: `photos:{チーム英字名}` → 写真配列のみ格納
   - これにより進捗データの読み書き速度を維持