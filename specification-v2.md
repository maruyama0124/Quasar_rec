# 謎解きレクリエーションアプリ 仕様書 v2（子ども食堂イベント版）

## 0. このドキュメントについて

旧仕様（大学サークル合宿向け / `specification.md`）からの **大幅な仕様変更** をまとめたものです。
対象が **子ども食堂のイベント参加者（子ども中心）** に変わり、それに伴って
チーム構成・ゲームの達成目標・写真枚数・デザイン・データ保存方式を変更します。

> このv2が確定したら、`specification.md` を置き換える想定です。

---

## 1. 変更点サマリー（旧 → 新）

| 項目 | 旧仕様 | 新仕様（v2） |
|------|--------|--------------|
| 対象ユーザー | 大学サークル（約50名・大学生） | 子ども食堂イベント（子ども中心） |
| チーム数 | 9チーム | **6チーム** |
| 暗号入力の報酬 | スクショ画像（7枚） | **文字パーツ（「みなみながさき」の1文字）** |
| 最終目標 | スクショ7枚を並べてクイズ完成 | **文字を並べて「みなみながさき」を完成** |
| 記念写真の枚数 | 7枚 | **4枚** |
| デザイン | ダークネイビー＋ゴールドのミステリー調 | **明るくポップな子ども向け** |
| アイコン素材 | 絵文字 | **絵文字は使わず、フリー素材画像（いらすとや等）** |
| データ保存 | `window.storage`（端末ローカル完結） | **Firebase（リアルタイムでチーム内共有）** |
| 開発言語 | JavaScript | **JavaScript のまま**（ユーザーのフォーム入力が少なくTS不要） |

---

## 2. ゲームの流れ（v2）

```
[1] チーム選択（初回起動時に必ず表示）
 ↓
[2] 会場内で主催者を探す（アプリ外のリアル行動）
 ↓
[3] 主催者のクイズに正解 → 一緒に記念撮影（手のポーズが次のヒント）
 ↓
[4] ヒントをもとに会場のどこかの封筒を探す（アプリ外）
 ↓
[5] 記念写真をアプリにアップロード（4枚まで）
 ↓
[6] 封筒の暗号をアプリに入力 → 正しければ「みなみながさき」の文字を1つ獲得
 ↓
[7] [2]〜[6] を繰り返して全7文字（み・な・み・な・が・さ・き）を集める
 ↓
[8] 集めた文字カードをドラッグ&ドロップで並べ替え → 「みなみながさき」を完成させる
 ↓
[9] 完成した言葉を主催者に伝える → クリア
```

> **判定方針（旧仕様を踏襲）:** 並べ替えの正誤・最終回答の正誤は **アプリ内では判定せず、主催者が目視で確認** する運用。

---

## 3. チーム構成（6チーム）

| # | チーム名 | チームキー（英字 / Firebase・storageキー用） |
|---|---------|------------------------------------------------|
| 1 | チームりさ | `risa` |
| 2 | チームつばさ | `tsubasa` |
| 3 | チームむた | `muta` |
| 4 | チームこはる | `koharu` |
| 5 | チームえいと | `eito` |
| 6 | チームしおり | `shiori` |

- 子ども向けに、ボタンは大きく・文字は大きく・タップしやすく。

### 3-1. チームの入り方（URL直入り + 選択画面のハイブリッド）

**運用前提:** 子ども（や付き添い）が **各自のスマホ** を持ち、同じチームの全員でリアルタイム同期する（1チーム複数端末）。

入口を2通り用意する：

| 方法 | 挙動 |
|------|------|
| **URL直入り（推奨）** | `https://(配信先)/?team=risa` のように **URLにチームキー** を含めてアクセスすると、チーム選択画面をスキップしてそのチームの画面へ直行する。チームごとに **QRコードを配布** すればワンタップで参加できる |
| **チーム選択画面** | URLに `team` が無い場合は、初回起動時にチーム選択画面を表示する（6チームから選ぶ） |

- URLの `team` の値（`risa` 等）を **そのまま Firestore のドキュメントキー** として使う。これにより「同じURLを開いた人＝同じチーム＝同じデータを同期」という関係がシンプルに成立する。
- 不正・未知の `team` 値の場合はチーム選択画面にフォールバックする。
- 選択したチームは `window.history`（URL書き換え）にも反映し、リロードしても同じチームを維持する。

---

## 4. 文字集め（旧「スクショ収集」の置き換え）

### 4-1. 合言葉と最終目標

- 集める対象は **「みなみながさき」の7文字**：`み` `な` `み` `な` `が` `さ` `き`
- 暗号（合言葉）は **全4つ**。**1回の暗号入力につき複数の文字をまとめて獲得** し、計4回の入力で7文字すべてが揃う。
- 文字数の配分は **2 + 2 + 2 + 1 = 7**。
- 全7文字を集めたら、並べ替えフェーズへ移行する。
- 並べ替えで **「みなみながさき」の正しい順番** を作るのがゴール。
- **難易度の工夫:** 1つの暗号で得る2文字は、**正解順で隣り合わない（離れた）組み合わせ** にする。
  もらった2文字がそのまま並び順のヒントにならないようにするため。
  さらに、**獲得した2文字を表示する並びも正解順を示さない**（順不同・シャッフル表示）。

### 4-2. 暗号マスターデータ（CODE_MAP / 合言葉4つ・複数文字版）

文字には重複（`み`が2つ、`な`が2つ）があるため、各パーツに
**正解の並び順を表す `id`（1〜7）** を持たせて区別する。
1つの暗号は **複数の文字パーツ（`chars` 配列）** を返す。

合言葉は確定済み（正規化後の「カタカナ」表記でキーにする）。
各暗号の2文字は **正解順（id）で隣接しない** よう割り当てる：

| 入力（合言葉） | 正規化後キー | 獲得する文字パーツ（正解id） |
|----------------|--------------|-------------------------------|
| はなれ | `ハナレ` | み(id1), さ(id6) |
| フーチークーチー | `フーチークーチー` | な(id2), が(id5) |
| ムッシュ | `ムッシュ` | み(id3), き(id7) |
| マサラハット | `マサラハット` | な(id4) |

```javascript
// キーは「大文字・カタカナ」で記述（正規化ルールに合わせる）。1暗号 = chars 配列で複数文字。
// 各暗号の2文字は正解順で離れた組み合わせ（並び順のヒントにならないようにするため）。
const CODE_MAP = {
  "ハナレ":         { chars: [{ id: 1, char: "み" }, { id: 6, char: "さ" }] },
  "フーチークーチー": { chars: [{ id: 2, char: "な" }, { id: 5, char: "が" }] },
  "ムッシュ":       { chars: [{ id: 3, char: "み" }, { id: 7, char: "き" }] },
  "マサラハット":   { chars: [{ id: 4, char: "な" }] },
};
// 正解順 = id昇順 → み(1) な(2) み(3) な(4) が(5) さ(6) き(7) = 「みなみながさき」
// ※ 獲得時に表示する2文字の並びも、正解順を示さないようシャッフルする。
```

> **割り当て注記:** 各暗号の2文字を正解順で離れた組み合わせにし、全idで 1〜7 を過不足なくカバー（配分 2-2-2-1）。
> 謎解きの都合で入れ替えたい場合は `CODE_MAP` の対応を差し替えるだけでよい（その際も非隣接を維持推奨）。

### 4-3. 暗号入力の正規化（旧仕様を踏襲）

`trim()` → 全角英数→半角 → 英字小文字→大文字 → ひらがな→カタカナ。
これにより表記ゆれを吸収して `CODE_MAP` のキーと照合する。

### 4-4. 入力時のフィードバック（子ども向けにポジティブに）

- **一致 & 未獲得:** 「あたり！」のような明るい演出 → その暗号の **複数文字をまとめて** 大きく表示 → 進捗に追加（「○もじゲット！」）
- **一致 & 獲得済み:** 「このあいことばはもうゲットしてるよ」
- **不一致:** 「ちがうみたい。もういちどさがしてみよう！」（責めない文言）
- **全7文字到達時（＝4つの暗号すべて入力）:** phaseを `arranging` に更新し、並べ替え画面への導線を表示

---

## 5. 記念写真（4枚に変更）

- フォトスロットを **7枠 → 4枠** に変更。
- 撮影/アップロードの仕組みは旧仕様を踏襲：
  - `<input type="file" accept="image/*" capture="environment">` でカメラ/ライブラリから取得
  - クライアント側で長辺600px以下にリサイズ + JPEG品質0.7に圧縮
- ただし保存先は **Firebase Storage** に変更（後述）し、同じチームの全員にリアルタイム反映する。
- 写真はゲーム進行のロック条件にはしない（旧仕様踏襲。撮影と暗号入力は独立して操作可能）。
- 写真タップで拡大表示、再撮影（差し替え）も可能。

---

## 6. 並べ替え画面（文字カード版）

- 集めた **7枚の文字カード** を縦に並べ、ドラッグ&ドロップで順番を入れ替える。
- ドラッグは **touchイベントで自前実装**（HTML5 DnD APIはモバイルSafari非対応のため。旧仕様踏襲）。
  - ドラッグハンドルからのみ開始、カード本体タップは拡大表示に割り当て。
- 並び順が変わるたびに Firebase に自動保存（チーム内で共有）。
- 「これでけってい！」ボタン → 「おとなのひと（主催者）にみせてね！」と表示。
- カードに番号は振らない（順番を推理させる）。

---

## 7. デザイン方針（子ども向けポップ）

### 7-1. トーン

- 明るく・あたたかく・楽しい。丸みのある形、大きめのボタンと文字。
- 旧仕様のダークミステリー調は廃止。

### 7-2. カラーパレット（提案）

| 用途 | 色（案） | イメージ |
|------|----------|----------|
| 背景 | クリーム/淡い水色のグラデーション | 明るい・やさしい |
| アクセント1 | オレンジ `#ff9f43` | 元気・楽しい |
| アクセント2 | スカイブルー `#54a0ff` | さわやか |
| 成功 | グリーン `#10ac84` | せいかい！ |
| 注意 | やわらかい赤 `#ee5253` | やさしい警告 |
| カード | 白 + パステルのふちどり + 角丸 + やわらかい影 | カードらしさ |

### 7-3. フォント・文言

- 丸ゴシック系の親しみやすいフォント。
- 文言はすべて **ひらがな多め・やさしい言葉**（漢字には注意、必要なら振り仮名）。

### 7-4. アイコン・イラスト素材

- **絵文字は使わない。**
- アイコンやイラストが必要な箇所は、**フリー素材（いらすとや等）をウェブ検索で取得して使用する。**
  - 使用候補：チーム選択のキャラ、正解時のお祝いイラスト、カメラ/封筒/かぎ等のアイコン、背景の飾り。
  - 素材の具体選定とライセンス確認は **実装フェーズで実施**（いらすとやは商用・非商用問わず無料、ただし規約上の点数制限等は要確認）。
- 取得した画像はリポジトリに同梱、または最適化のうえ配信する。

### 7-5. アニメーション

- 画面遷移はやさしいフェード。
- 正解・文字獲得時はカードがポンと出る楽しい動き（scale + バウンド）。

---

## 8. データ設計（Firebase / リアルタイム共有）

旧仕様の `window.storage`（端末ローカル）から、**Firebase によるチーム内リアルタイム共有** に変更する。
同じチームの複数端末で、写真と進捗（獲得文字・並び順）が即座に同期される。

### 8-1. 使用サービス

| 用途 | Firebaseサービス |
|------|------------------|
| 進捗データ（獲得文字・並び順・phase） | **Cloud Firestore**（リアルタイムリスナーで同期） |
| 記念写真の画像本体 | **Firebase Storage**（アップロードしURLを参照） |
| 端末識別（任意） | **Anonymous Authentication**（匿名ログイン） |

> 画像はBase64で持たず、Storageにアップロードして **ダウンロードURLをFirestoreに保存** する方式に変更
> （旧仕様のBase64埋め込みはチーム共有・容量の観点で不向きなため）。

### 8-2. Firestore データ構造（案）

```
teams (collection)
  └─ {teamKey} (document)   // 例: "risa"
       teamName:     "チームりさ"
       unlockedChars: [                 // 獲得済みの文字パーツ
         { id: 1, char: "み" },
         { id: 5, char: "が" }
       ]
       order:        [1, 5, 3, ...]     // 並べ替え中の id 順
       phase:        "collecting"       // "collecting" | "arranging"
       photos:       [                  // 記念写真（最大4枚）
         { url: "https://.../xxx.jpg", uploadedAt: <timestamp> },
         null, null, null
       ]
       updatedAt:    <serverTimestamp>
```

- クライアントは `onSnapshot` で `teams/{teamKey}` を購読し、変更を即座にUIへ反映。
- 書き込みは該当フィールドの更新（写真追加・文字獲得・並び替え）。

### 8-3. Storage 構成（案）

```
photos/{teamKey}/{slotIndex}.jpg   // 圧縮済みJPEGをアップロード
```

### 8-4. セキュリティルール（厳格方針）

緩いテスト用ルール（`allow read, write: if true;`）は **使わない**。以下を満たす厳格なルールにする。

**Firestore ルールの要件**
- **匿名認証を必須**（`request.auth != null`）。未認証アクセスは全拒否。
- 書き込み対象は `teams/{teamKey}` のみ。`teamKey` は **既知の6チーム（risa/tsubasa/muta/koharu/eito/shiori）のホワイトリスト** に限定。
- **スキーマ検証**：`phase` は `collecting | arranging` のみ、`photos` は長さ4以下、`unlockedChars`・`order` は配列であること等を検証。想定外フィールドの混入を弾く。
- 上記以外のすべてのパスは **deny**。

```javascript
// firestore.rules（要点）
rules_version = '2';
service cloud.firestore {
  match /databases/{db}/documents {
    function signedIn() { return request.auth != null; }
    function knownTeam(id) {
      return id in ['risa','tsubasa','muta','koharu','eito','shiori'];
    }
    match /teams/{teamKey} {
      allow read: if signedIn() && knownTeam(teamKey);
      allow write: if signedIn() && knownTeam(teamKey)
        && request.resource.data.phase in ['collecting','arranging']
        && request.resource.data.photos.size() <= 4
        && request.resource.data.unlockedChars is list
        && request.resource.data.order is list;
    }
    match /{document=**} { allow read, write: if false; }
  }
}
```

**Storage ルールの要件**
- 匿名認証必須。`photos/{teamKey}/{slot}` のみ許可（`teamKey` ホワイトリスト、`slot` は 0〜3）。
- アップロードは **画像のみ**（`request.resource.contentType.matches('image/.*')`）かつ **サイズ上限**（例: 1MB）を強制。

```javascript
// storage.rules（要点）
rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /photos/{teamKey}/{slot} {
      allow read: if request.auth != null;
      allow write: if request.auth != null
        && teamKey in ['risa','tsubasa','muta','koharu','eito','shiori']
        && request.resource.contentType.matches('image/.*')
        && request.resource.size < 1 * 1024 * 1024;
    }
    match /{allPaths=**} { allow read, write: if false; }
  }
}
```

> **補足:** クライアント完結ゲームのため、悪意ある改ざんを完全には防げない（正解判定は最終的に主催者の目視）。
> 上記ルールは「未認証・想定外パス・巨大/非画像ファイル・未知チームの拒否」を担保するもので、イベント用途には十分。
> 公開期間終了後はルールをさらに締める or プロジェクトを停止する。

### 8-5. オフライン挙動の注意

- 旧仕様はオフライン動作可能だったが、v2は **共有のため通信前提** になる。
- 会場のネット環境が不安定な場合に備え、Firestoreのオフライン永続化（キャッシュ）を有効化し、
  復帰時に同期されるようにする。

---

## 9. 主催者の進捗把握

- **専用のモニター画面は作らない。**
- 主催者は **Firebase コンソール（Firestore データビュー）** で `teams` コレクションを見れば、各チームの
  獲得文字・写真・phase を直接確認できるため、それで運営する。

---

## 10. 非機能要件（v2）

| 項目 | 要件 |
|------|------|
| 対応ブラウザ | iOS Safari 16+ / Android Chrome 最新版 |
| 画面サイズ | スマホ縦（375〜430px幅）を主対象、子どもが操作しやすい大きめUI |
| 通信 | Firebase との通信前提（オフライン時はキャッシュで一部継続 → 復帰時同期） |
| 端末構成 | **各自スマホ**（1チームに複数端末）。同じチームの全員でリアルタイム同期 |
| 同時利用 | 6チーム × 各数名（合計数十端末程度）。Firebase無料枠（Sparkプラン）で十分想定 |
| タッチ操作 | ボタン最小44×44px以上、子ども向けにさらに大きめ推奨。ドラッグはハンドルで開始 |
| 画像容量 | 4枚 × 圧縮後 約50〜100KB |

---

## 11. 実装タスク概要（旧コードからの差分）

1. **チーム定義の変更** — `TEAMS` を6チーム（risa/tsubasa/muta/koharu/eito/shiori）に。
2. **CODE_MAP の合言葉4つ・複数文字版化** — 画像Base64 → `{ chars: [{ id, char }] }` 構造（ハナレ/フーチークーチー/ムッシュ/マサラハット）。
3. **URL直入り対応** — `?team=xxx` を解釈し、あれば選択画面スキップ。未知/無しなら選択画面へ。
4. **獲得物の表示変更** — スクショ画像表示 → 文字カード表示に（1暗号で複数文字をまとめて獲得）。
5. **写真スロットを4枠に** — 7→4へ。保存先をFirebase Storageへ。
6. **並べ替え画面を文字カード対応に** — 画像カード → 文字カード（touch DnDロジックは流用）。
7. **Firebase導入** — 初期化、Firestore購読（`onSnapshot`）、Storageアップロード、匿名認証、**厳格なセキュリティルール**（第8-4）。
   - `window.storage` 依存箇所をFirebase読み書きへ置き換え。
8. **デザイン全面刷新** — ポップな配色・丸ゴシック・やさしい文言、絵文字撤廃。
9. **フリー素材の調達** — いらすとや等からウェブ検索で取得し、アイコン/イラストを差し込み（ライセンス確認）。

---

## 12. 要確認事項（このv2で前提を置いた点）

**【確定済み】**
- 文字数 = 7（「みなみながさき」）。**暗号は4つ**、1回で複数文字獲得（配分 2-2-2-1）。
- 共有 = 各自スマホで写真＋進捗をチーム内同期。入口はURL直入り（`?team=xxx`／QR配布）＋選択画面のハイブリッド。
- 主催者モニター画面は **作らない**（Firebaseコンソールで把握）。
- Firebaseセキュリティルールは **厳格**（第8-4）。
- 合言葉 = はなれ / フーチークーチー / ムッシュ / マサラハット。
- 1暗号で得る2文字は **正解順で隣接しない組み合わせ**、表示順もシャッフル（並び順のヒントにしない）。

**【残りの仮置き・要確認】**
1. **文字の割り当て** — 非隣接になるよう仮置き（ハナレ=み/さ, フーチークーチー=な/が, ムッシュ=み/き, マサラハット=な）。組み合わせの希望があれば。
2. **チームキーの英字表記**（risa/tsubasa/muta/koharu/eito/shiori）はこちらで命名。Firebase/URL用に問題ないか。
3. **フリー素材の具体選定** は実装フェーズで実施（いらすとや等の規約・点数制限を確認の上）。
4. **Firebaseプロジェクト** の作成・APIキー等の設定情報は、実装着手時に必要（こちらでは用意できないため共有が必要）。
